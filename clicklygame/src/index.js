import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css";
// import the index for the main web page and them use react dom to render them to the page
ReactDOM.render(<App />, document.getElementById("root"));
